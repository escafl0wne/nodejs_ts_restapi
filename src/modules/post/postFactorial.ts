// import { parentPort } from "worker_threads";

// const f = (number: number):number => {
//     if(number <= 1) return 1;
//     return number * f(number - 1);
// }

// parentPort?.postMessage(f(process.argv[2]));
